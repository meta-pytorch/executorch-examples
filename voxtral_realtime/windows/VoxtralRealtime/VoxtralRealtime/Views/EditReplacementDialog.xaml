<!-- Copyright (c) Meta Platforms, Inc. and affiliates. -->
<!-- All rights reserved. -->
<!-- This source code is licensed under the BSD-style license found in the -->
<!-- LICENSE file in the root directory of this source tree. -->

<Window x:Class="VoxtralRealtime.Views.EditReplacementDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Replacement" Width="400" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <StackPanel Margin="20">
        <Label x:Name="TriggerLabel" Content="Trigger:" FontSize="13"/>
        <TextBox x:Name="TriggerBox" FontSize="13" Padding="6,4" Margin="0,0,0,12"/>

        <Label x:Name="ReplacementLabel" Content="Replacement:" FontSize="13"/>
        <TextBox x:Name="ReplacementBox" FontSize="13" Padding="6,4" Margin="0,0,0,12"/>

        <CheckBox x:Name="CaseSensitiveCheck" Content="Case sensitive"
                  FontSize="12" Margin="0,0,0,8"/>
        <CheckBox x:Name="WordBoundaryCheck" Content="Require word boundary"
                  FontSize="12" IsChecked="True" Margin="0,0,0,16"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource ToolbarButton}"
                    Click="OnCancel" Padding="16,6" Margin="0,0,8,0"/>
            <Button Content="Save" Style="{StaticResource PrimaryButton}"
                    Click="OnSave" Padding="16,6"/>
        </StackPanel>
    </StackPanel>
</Window>
